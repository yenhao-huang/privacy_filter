Deprecated evaluation scripts kept for reference.

The active evaluation entry point is:

```bash
.venv/bin/python -m core.api.eval_by_masked_str_match
```

Deprecated scripts:

- `evaluate_openai_privacy_filter.py`: exact span boundary match for OpenAI token labels.
- `evaluate_vllm_masked.py`: per-label placeholder count for masked text.
